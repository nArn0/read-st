name: Build

on:
  workflow_dispatch:
  push:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v6
    - name: Setup Golang
      uses: actions/setup-go@v6
      with:
        go-version: '1.25.6'
    - name: Cross build CLI
      run: make cross
    - name: Release
      uses: softprops/action-gh-release@v2
      if: startsWith(github.ref, 'refs/tags/')
      with:
        files: |
          read-st-linux-amd64
          read-st-windows-amd64.exe
          read-st-darwin-amd64
          read-st-darwin-arm64

